{
  "name": "himalix-labs-master",
  "version": "1.0.0",
  "description": "Master runner execution scripts for Himalix Labs",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
